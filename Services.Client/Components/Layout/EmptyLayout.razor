@inherits LayoutComponentBase

<div class="client-degrade-background min-vh-100 d-flex flex-column justify-content-center align-items-center">
    @Body
</div>

<style>
    /* Degradé corporativo oscuro idéntico al del inicio */
    .client-degrade-background {
        background: linear-gradient(135deg, #1b1535 0%, #0c0818 100%);
        min-height: 100vh;
        width: 100%;
    }
</style>